<Window x:Class="ModernZamanTakip.MiniZamanlayiciPenceresi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mini Sayac" Height="100" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" WindowStartupLocation="CenterScreen">

    <Border Background="#D00A0A0C" CornerRadius="20" MouseLeftButtonDown="Border_MouseLeftButtonDown" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Viewbox Margin="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="lblMiniSayac" Grid.Column="0" Text="00:00" Foreground="White" FontSize="36" FontWeight="Light" VerticalAlignment="Center" Margin="10,0,20,0"/>
                    <Button x:Name="btnMiniOynat" Grid.Column="1" Content="⏸" FontSize="24" Foreground="#00E676" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" Click="btnMiniOynat_Click" VerticalAlignment="Center"/>
                    <Button x:Name="btnBuyut" Grid.Column="2" Content="⤢" FontSize="24" Foreground="#A0A0A5" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" Click="btnBuyut_Click" VerticalAlignment="Center"/>

                    <Button x:Name="btnKompleKapat" Grid.Column="3" Content="✕" FontSize="20" Foreground="#FF3B30" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnKompleKapat_Click" VerticalAlignment="Center"/>
                </Grid>
            </Viewbox>

            <Thumb Width="25" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" DragDelta="Thumb_DragDelta" Margin="0,0,-5,-5">
                <Thumb.Template>
                    <ControlTemplate>
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Path Data="M 12,18 L 18,12 M 6,18 L 18,6" Stroke="#50FFFFFF" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" IsHitTestVisible="False"/>
        </Grid>
    </Border>
</Window>